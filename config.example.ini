; Example configuration file for Weather API
; Copy to config.ini and fill in your real API key only for local development,
; or use the OPENWEATHER_API_KEY environment variable / .env instead.

[openweathermap]
api=
